Flask==3.0.3  # Downgraded from 3.1.1 for stability
flask-cors==4.0.1  # Older stable version
gunicorn==22.0.0  # Downgraded for compatibility
pytz==2024.1  # Slightly older to match common deployments